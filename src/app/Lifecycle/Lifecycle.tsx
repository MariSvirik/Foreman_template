import * as React from 'react';
import {
  PageSection,
  TextContent,
  Title,
} from '@patternfly/react-core';

const Lifecycle: React.FunctionComponent = () => (
  <PageSection>
    <TextContent>
      <Title headingLevel="h1" size="2xl">Lifecycle</Title>
    </TextContent>
  </PageSection>
);

export { Lifecycle };
